[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "canonical-common"
version = "0.0.0"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["canonical_common"]